{
  "name": "trickdex-landing",
  "version": "1.0.0",
  "private": true,
  "description": "www.wakeboard.com — the public wakeboard trick list.",
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "node scripts/gen-redirects.mjs && next build",
    "gen:redirects": "node scripts/gen-redirects.mjs",
    "start": "next start -p 3001",
    "lint": "next lint",
    "snapshot": "node scripts/snapshot-tricks.mjs",
    "test": "next build && playwright test",
    "test:only": "playwright test"
  },
  "dependencies": {
    "next": "^15.5.25",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "undici": "^7.29.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "@types/node": "22.10.7",
    "@types/react": "19.0.7",
    "@types/react-dom": "19.0.3",
    "typescript": "5.7.3"
  }
}
